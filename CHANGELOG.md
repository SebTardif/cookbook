# Changelog

## Unreleased

- Keep the coding-agent CLI responsive for cancellation, including startup, retry, and terminal shutdown; thanks @SebTardif.

- Fix standalone SDK examples so compiled `pnpm start` runs against the cookbook SDK shim.
- Use canonical public model identifiers in SDK shim and Run Board fixtures.
